package textExcel;

public class PercentCell extends RealCell{

	private String input;
	
	public PercentCell(String value) {
		super(value);
	}
	

	@Override
	public String abbreviatedCellText() {
		String store= input.substring(0, input.indexOf("."));
		if (store.length()>10){
		String percentage= super.getDoubleValue() + "%";
		percentage += "          ";
		return percentage.substring(0,10);
		}
		else { 
			return store +"%";
			
		}
}

	@Override
	public double getDoubleValue(){
		return Double.parseDouble(super.fullCellText().substring(0, super.fullCellText().length()-2));
	}
	//get rid of the percent sign at the end 
	
	@Override
	public String fullCellText() {
		return (getDoubleValue()/100.0 + "");
	}
}
